doctype html
html
  head
    //- interpolation (template string #{var})
    title Natours | #{tour}
    link(rel="stylesheet" href="css/style.css")
    link(rel="shortcut icon" type="image png" href="img/favicon.png")

  body
    h1= tour
    // comment visible in html
    //- comment  NOT visible in html (only for Pug)

    //-JS in Pug
    h2= user.toUpperCase()


    - const x = 9;
    h2= 2 * x

    p This just some p text